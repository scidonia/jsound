{
  "type": "object",
  "properties": {
    "id": {"type": "integer"},
    "title": {"type": "string"},
    "body": {"type": "string"},
    "event": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": {"type": "string", "enum": ["opened"]},
            "author": {"type": "string"},
            "timestamp": {"type": "string"},
            "milestone": {"type": "string"}
          },
          "required": ["type", "author", "timestamp", "milestone"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {"type": "string", "enum": ["assigned"]},
            "assignee": {"type": "string"},
            "assigned_by": {"type": "string"}
          },
          "required": ["type", "assignee", "assigned_by"],
          "additionalProperties": false
        }
      ]
    }
  },
  "required": ["id", "title", "event"],
  "additionalProperties": false
}