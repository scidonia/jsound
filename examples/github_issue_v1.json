{
  "type": "object",
  "properties": {
    "id": {"type": "integer"},
    "title": {"type": "string"},
    "body": {"type": "string"},
    "event": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": {"type": "string", "enum": ["opened"]},
            "author": {"type": "string"},
            "timestamp": {"type": "string"}
          },
          "required": ["type", "author", "timestamp"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {"type": "string", "enum": ["closed"]},
            "reason": {"type": "string", "enum": ["completed", "not_planned"]},
            "closed_by": {"type": "string"}
          },
          "required": ["type", "reason", "closed_by"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {"type": "string", "enum": ["labeled"]},
            "label": {"type": "string"},
            "added_by": {"type": "string"}
          },
          "required": ["type", "label", "added_by"],
          "additionalProperties": false
        }
      ]
    }
  },
  "required": ["id", "title", "event"],
  "additionalProperties": false
}